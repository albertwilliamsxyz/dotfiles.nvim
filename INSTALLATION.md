voice.nvim
brew install sox
openai-whisper (for local models) (investigate alternatives)
pipx to manage dependencies like npx for pip
brew install ffmpeg

copilot
requires node up to date (v22.22 at the time)
node version manager (nvm) to manage versions

image.nvim
requires image-magick
